# Security policy

Do not report vulnerabilities involving real documents, credentials, or production signing through public issues.

Until a private disclosure address is published, create a minimal GitHub Security Advisory without secrets or personal data.

The prototype must never receive production private keys or reusable e-signature credentials. Browser credentials must be injected by a vault outside the model context. Approval evidence must be bound to the final content hash and execution parameters.

